'use client';
'use strict';

var React = require('react');
var recharts = require('recharts');
var core = require('@mantine/core');
var RadarChart_module = require('./RadarChart.module.css.cjs');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var React__default = /*#__PURE__*/_interopDefault(React);

const defaultProps = {
  withPolarGrid: true,
  withPolarAngleAxis: true,
  withPolarRadiusAxis: false
};
const varsResolver = core.createVarsResolver((theme, { gridColor, textColor }) => ({
  root: {
    "--chart-grid-color": gridColor ? core.getThemeColor(gridColor, theme) : void 0,
    "--chart-text-color": textColor ? core.getThemeColor(textColor, theme) : void 0
  }
}));
const RadarChart = core.factory((_props, ref) => {
  const props = core.useProps("RadarChart", defaultProps, _props);
  const {
    classNames,
    className,
    style,
    styles,
    unstyled,
    vars,
    data,
    series,
    dataKey,
    gridColor,
    textColor,
    radarProps,
    radarChartProps,
    polarGridProps,
    polarAngleAxisProps,
    polarRadiusAxisProps,
    withPolarGrid,
    withPolarAngleAxis,
    withPolarRadiusAxis,
    children,
    ...others
  } = props;
  const theme = core.useMantineTheme();
  const getStyles = core.useStyles({
    name: "RadarChart",
    classes: RadarChart_module,
    props,
    className,
    style,
    classNames,
    styles,
    unstyled,
    vars,
    varsResolver
  });
  const radars = series.map((item, index) => /* @__PURE__ */ React__default.default.createElement(
    recharts.Radar,
    {
      key: index,
      name: item.name,
      dataKey: item.name,
      stroke: core.getThemeColor(item.strokeColor || item.color, theme),
      fill: core.getThemeColor(item.color, theme),
      fillOpacity: item.opacity || 0.4,
      isAnimationActive: false,
      ...typeof radarProps === "function" ? radarProps(item) : radarProps
    }
  ));
  return /* @__PURE__ */ React__default.default.createElement(core.Box, { ref, ...getStyles("root"), ...others }, /* @__PURE__ */ React__default.default.createElement(recharts.ResponsiveContainer, { ...getStyles("container") }, /* @__PURE__ */ React__default.default.createElement(recharts.RadarChart, { data, ...radarChartProps }, withPolarGrid && /* @__PURE__ */ React__default.default.createElement(recharts.PolarGrid, { stroke: "var(--chart-grid-color)", ...polarGridProps }), withPolarAngleAxis && /* @__PURE__ */ React__default.default.createElement(recharts.PolarAngleAxis, { dataKey, ...polarAngleAxisProps }), withPolarRadiusAxis && /* @__PURE__ */ React__default.default.createElement(recharts.PolarRadiusAxis, { stroke: "var(--chart-grid-color)", ...polarRadiusAxisProps }), radars, children)));
});
RadarChart.displayName = "@mantine/charts/RadarChart";
RadarChart.classes = RadarChart_module;

exports.RadarChart = RadarChart;
//# sourceMappingURL=RadarChart.cjs.map
