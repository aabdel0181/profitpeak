'use client';
'use strict';

var React = require('react');
var recharts = require('recharts');
var core = require('@mantine/core');
require('../AreaChart/AreaChart.cjs');
var AreaGradient = require('../AreaChart/AreaGradient.cjs');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var React__default = /*#__PURE__*/_interopDefault(React);

const classes = {};
const defaultProps = {
  withGradient: true,
  connectNulls: true,
  fillOpacity: 0.6,
  strokeWidth: 2,
  curveType: "linear"
};
function getTrendColor(data, trendColors) {
  const first = data[0];
  const last = data[data.length - 1];
  if (first < last) {
    return trendColors.positive;
  }
  if (first > last) {
    return trendColors.negative;
  }
  return trendColors.neutral || trendColors.positive;
}
const varsResolver = core.createVarsResolver(
  (theme, { color, data, trendColors }) => ({
    root: {
      "--chart-color": trendColors ? core.getThemeColor(getTrendColor(data, trendColors), theme) : color ? core.getThemeColor(color, theme) : void 0
    }
  })
);
const Sparkline = core.factory((_props, ref) => {
  const props = core.useProps("Sparkline", defaultProps, _props);
  const {
    classNames,
    className,
    style,
    styles,
    unstyled,
    vars,
    data,
    withGradient,
    fillOpacity,
    curveType,
    strokeWidth,
    trendColors,
    connectNulls,
    areaProps,
    ...others
  } = props;
  const getStyles = core.useStyles({
    name: "Sparkline",
    classes,
    props,
    className,
    style,
    classNames,
    styles,
    unstyled,
    vars,
    varsResolver
  });
  const id = React.useId();
  const mappedData = React.useMemo(() => data.map((value, index) => ({ value, index })), [data]);
  return /* @__PURE__ */ React__default.default.createElement(core.Box, { ref, ...getStyles("root"), ...others, dir: "ltr" }, /* @__PURE__ */ React__default.default.createElement(recharts.ResponsiveContainer, null, /* @__PURE__ */ React__default.default.createElement(recharts.AreaChart, { data: mappedData }, /* @__PURE__ */ React__default.default.createElement(
    recharts.Area,
    {
      dataKey: "value",
      type: curveType,
      fill: `url(#${id})`,
      stroke: "var(--chart-color, var(--mantine-color-blue-filled))",
      isAnimationActive: false,
      connectNulls,
      strokeWidth,
      fillOpacity: 1,
      ...areaProps
    }
  ), /* @__PURE__ */ React__default.default.createElement("defs", null, /* @__PURE__ */ React__default.default.createElement(
    AreaGradient.AreaGradient,
    {
      id,
      color: "var(--chart-color, var(--mantine-color-blue-filled))",
      fillOpacity,
      withGradient
    }
  )))));
});
Sparkline.displayName = "@mantine/charts/Sparkline";
Sparkline.classes = classes;

exports.Sparkline = Sparkline;
//# sourceMappingURL=Sparkline.cjs.map
